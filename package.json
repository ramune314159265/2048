{
  "scripts": {
    "lint": "eslint src",
    "fix": "eslint src --fix",
    "build": "node build.mjs"
  },
  "dependencies": {
    "esbuild": "^0.25.1",
    "fs-extra": "^11.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "eslint": "^9.22.0",
    "globals": "^16.0.0"
  }
}
